const {Router} = require("express");

const router = Router();

router.get("/api/v1/todos",)
router.get("/api/v1/todos/:id",)
router.post("/api/v1/todos",)
router.put("/api/v1/todos/:id",)
router.delete("/api/v1/todos/:id",)

module.exports = router;